

export const imagesReducer = ( state = {}, action ) => {
    switch ( action.type ) {

        case 43:
            
            return state;
    
        default:
            return state;
    }
}
